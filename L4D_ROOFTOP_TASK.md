# 目标文档 —— 《求生之路楼顶开局》3D Demo（多 Agent 公平测试版）

> 本文件是在原 `goal.md`（备份为 `goal.original.md`）基础上修订的版本，**专用于多 Agent 公平对比测试**。它同时是「产品需求」与「执行规范」，两个身份缺一不可。
> 修订目标：在保留原有场景氛围与玩法诉求的同时，内建**公平、可复用、可客观评分**的执行约束，消除多 Agent 并发导致的相互污染。

---

## 0. 公平测试执行约束（FAIR-PLAY CONTRACT）——所有 Agent 必须遵守

以下规则是为了让各 Agent 的产出**可比、不互相干涉、能自动验收**。违反任一规则视为「环境作弊」，该项不得分。

1. **独立命名空间（唯一性要求）**
   每个 Agent 被分配一个固定的运行标记 `<AGENT>`。Agent 产出的**一切文件、目录、Blender 场景对象名、导出文件名、URL 路由**都必须以前缀 `${AGENT}_` 开头，例如：
   - 目录：`out/${AGENT}_rooftop/`
   - Blender 场景对象：`${AGENT}_door`、`${AGENT}_zombie`
   - 导出文件：`${AGENT}_rooftop.glb`、`${AGENT}_zombie.glb`
   - 构建入口：`out/${AGENT}/index.html`
   禁止读写其他 Agent 的命名空间。

2. **Blender 并发约定（关键）**
   - 所有 Agent 通过同一个 Blender MCP 服务（`localhost:9876`）访问 **同一个** Blender 实例（GUI 模式，addon 硬性禁止 `--background`）。
   - **每个 Agent 最多执行一次 `clear_scene` 或等价清理**，然后只在**自己的对象前缀**下创建/改名对象；**禁止删除或修改其他前缀的对象**。
   - Blender 只用于**静态场景**建模与导出 GLB。**骨骼动画一律不用 Blender**（见 §3 双路径），以避免多 Agent 并发在 GUI 主线程上互相抢占。
   - 一旦完成导出，Agent **立即释放** Blender 交互（不再改动场景），转入 three.js 代码阶段。

3. **可复现构建**
   交付物必须提供一条**幂等构建命令**：在干净 checkout 上运行后能产出可运行的网页，例如 `npm build && npm start` 或 `python -m http.server`。任何「依赖我手动开的窗口 / 依赖我当前 Blender 会话」的步骤都不可验收。

4. **自动验收脚本优先**
   每个 Agent 的交付目录必须包含一个可执行脚本 `verify.sh`（或等价），接收以下参数并能自动打分：
   - `verify.sh assets` —— 校验模型文件存在、GLB 可解析、包含预期对象。
   - `verify.sh run` —— 无头启动构建产物，加载成功不报错。
   - 评分项对应的脚本见 §0.5「自动验收表」。
   其余主观项（氛围、手感）由人工在浏览器内复核。

5. **任务输入一律一致**
   每个 Agent 拿到的是**同一份本文件** + 同一份基线仓库。不得使用 Agent 私下记忆或外部搜索引擎补足的"隐藏配置"。

---

## 1. 目标概述

做出一个简单但**有氛围**的《求生之路1》第一关「楼顶开局」迷你 3D Demo：three.js 负责渲染、交互与玩法；Blender 负责楼顶静态场景的低多边形建模与 glTF 导出；具备第一人称漫游、交互开门、敌人追踪、血量与简单流程引导。**是 Demo，不是完整关卡。**

## 2. 场景设定与元素（与原目标一致，仅补充可验证性）

- 主题：夜晚城市楼顶，雾 + 冷色调光，末日/生存恐怖氛围；参考 No Mercy 第一关顶楼（主楼顶、附属矮屋顶、小房间、楼梯间入口、通向下层的门/平台）。
- 元素：楼顶地面（排水沟/地砖/水泥裂缝贴图细节）、围栏/矮墙、空调机组、排风扇、管道、水塔、广告牌残架、出生点安全屋门、通往下一阶段的门或楼梯口（可暂时锁死作为流程终点）。
- 所有静态物体在 Blender 建立，并保证 **UV 展开 + 简单纹理**（2K 以下，含纹理集/重复贴图），导出为 glTF/GLB。

## 3. 建模与资源：双路径（关键修订）

### 路径 A —— Blender 静态场景（必做）
- 仅限**静态物**：屋顶地面、围栏、屋顶小屋、楼梯间入口、道具（空调机、水塔、管道等）、安全屋门框与门板。
- 关键可交互物（安全屋门）的 Pivot 放在**门铰链位置**，便于 three.js 绕 Pivot 旋转开门。
- 导出 `${AGENT}_rooftop.glb`；命名空间前缀遵守 §0.1。

### 路径 B —— 角色/敌人：推荐 three.js 代码实现（放弃 Blender 骨骼动画）
- 由于多 Agent 并发操作同一 Blender GUI 做骨骼动画不现实，**敌人模型与动画统一在 three.js 侧实现**：
  - 敌人几何体可用 `BoxGeometry` / `CapsuleGeometry` 组合成简化人形，或用 `GLTFLoader` 加载一个不含骨骼的静态模型。
  - 动画用 three.js 的 **`AnimationClip` + `KeyframeTrack`**（对节点做位置/旋转插值）实现两段循环：
    - `Idle`：站立轻微摇晃。
    - `Walk`：小跑、身体前倾、左右摆臂/摆腿（可用骨架 `Bone` 或直接对子 mesh 旋转）。
  - 也可选用库存低多边形带骨骼的模型，但动画必须在 three.js 侧用代码驱动，**不依赖 Blender 输出骨骼动画**。
- 说明：这一修订是为了保证「各 Agent 都能在主线程上独立、可复现地完成动画」，原目标中「Blender 制作 Idle/Walk 动画」由「three.js 代码动画」等价承接。

## 4. three.js 技术实现（核心，保持原目标 + 公平化）

1. **场景**：three.js `Scene`、透视相机、`WebGLRenderer`；`DirectionalLight` 模拟月光 + 若干点光/聚光作楼顶小灯；`FogExp2` 蓝灰雾。
2. **玩家控制**：`PointerLockControls` 或自定义 FPS；WASD + 鼠标视角；空格可选小跳；楼顶内 AABB/射线**碰撞阻挡**（不掉出地图、不穿墙）。
3. **交互逻辑**：
   - 安全屋门靠近按 `E` 绕 Pivot 旋转开。
   - 流程 UI：顶部显示当前目标文本（"离开安全屋→楼顶中央→楼梯口/终点"），接近触发区更新文本。
4. **敌人 AI**：
   - 出生点实例化 2–5 个敌人。
   - 初始 `Idle`；玩家进入 Sphere 半径切 `Walk` 并朝玩家移动（每帧求向量归一化*速度，保持 Y 朝向玩家）。
   - 接触降血（`health` 递减并显示）；血量到 0 显示"你已倒下"并冻结玩家移动。
5. **UI 与状态机**：左上角血条/数值；中下提示文本；状态机 `Start`（按任意键开始）→ `Playing` → `Dead`（可重新开始）。
6. **开场镜头（Cinematic Intro，贴近官方《求生之路》体验）**：
   - 游戏启动后（`Start` 状态），先播放一段**由远到近的电影式开场**：镜头从楼顶外远处（能看到楼顶全貌 + 夜晚城市天际线）平滑推近，穿过雾与夜色，最终落到安全屋门口 / 玩家出生点附近。
   - 推进过程可配合标题/字幕（如"求生之路 · 楼顶开局"淡入淡出）与轻微镜头缓动（ease-in-out），时长约 3–6 秒。
   - **不需要选人环节**（官方是选人后开局，本 Demo 明确不做选人 UI）；开场结束、玩家可点击"开始"后切到第一人称。
   - 实现建议：`Start` 状态下用独立相机动画（如沿曲线路径插值位置/朝向，或 `AnimationClip` 驱动 intro 相机），点击开始后平滑过渡到第一人称相机（可复用同一 `startGame` 流程）。
7. **模块化（ES Modules）**：渲染/相机/灯光、模型加载、玩家控制与碰撞、敌人 AI 与动画、UI 与游戏状态、开场镜头，至少拆成独立模块文件。

## 5. 性能与结构要求

- 控制面数，普通浏览器流畅；贴图 2K 以下，纹理集/重复贴图。
- 代码用 ES 模块，单一入口 `main.js`，无全局变量污染。
- 提供可复现构建（§0.3）。

## 5.5 质量基线（历史评审反馈固化，所有 Agent 必须满足，保证完成度可比）

以下条目来自历轮评审发现的问题，**对所有 Agent 一视同仁**（公平约束：不针对任何单一 Agent，全部写入共享任务书）：

1. **入口可加载**：`index.html` 引用的入口脚本必须真实存在且路径正确（注意 `${AGENT}_` 前缀文件名与实际一致）。若源码直接 `import 'three'` 等裸说明符，必须保留 importmap（skeleton 已提供，缺失会导致页面卡死在首页、模块加载失败）。交付前请用静态服务器 + 浏览器打开验证页面能进入游戏。
2. **光照可见性（重要）**：允许夜晚冷色调氛围，但**场景主体（地面/围栏/小屋）不得被大片纯黑覆盖**。必须提供暗部兜底补光：环境光（`AmbientLight`，如 `0x223355` 强度 ~0.9）或高强度的半球光（下半球色不要用近黑 `0x080b12`）。月光/方向光之外要有补光，背光面应可辨。判断标准：打开页面后，地面与建筑主体肉眼可辨，不是黑屏。
3. **场景丰富度**：静态场景对象类别 ≥6 类（见 A2），建议达到 8+ 类（门/围栏/小屋/楼梯口/空调/水塔/管道/广告牌/排风/杂物/灯具等）；材质应有粗糙度/金属度区分；贴图 2K 以下（程序化贴图亦可）。「最小可运行」不等于完成——完成度直接影响主观分 M1–M4。
4. **动画绑定正确**：three.js 动画轨道 `KeyframeTrack` 必须绑定**真实对象引用**（直接对 mesh/group 的 `position`/`quaternion` 建立轨道），不要用无法解析的 uuid 字符串路径（会 no-op 且无报错，表现上敌人不动）。自测时确认敌人 Idle/Walk 确实在播放。
5. **诚实披露**：未完成/受限项（简化模型、简化碰撞、无 PBR 等）在 `README` 与最终报告中如实列出，不作为隐藏缺陷。
6. **开场体验（官方风格）**：启动后必须有"由远到近"的电影式开场镜头（见 §4.6），不能直接切第一人称；开场动画需可见（镜头在动），点击开始后才进入 `Playing`。主观分 M1 会参考开场氛围。

## 6. 验收表（自动 + 人工）

### 6.1 自动验收项（Agent 必须通过 `verify.sh` 自证，独立评分，每项 0–2 分）
| # | 验收项 | 判定方式 |
|---|--------|----------|
| A1 | ${AGENT}_rooftop.glb 存在且可解析 | `parse` GLB，无报错 |
| A2 | GLB 内含预期静态对象（门、围栏、小屋、楼梯口等 **≥6 类**）| 遍历 GLB points 命名 |
| A3 | 构建产物 `index.html` 可加载，`.mjs` 模块无语法错误 | 无头 bundle/静态检查 |
| A4 | 代码含 PointerLock/FPS 控制逻辑与碰撞阻挡 | grep 关键函数 |
| A5 | 代码含门交互 E 键 + 旋转开门逻辑 | grep |
| A6 | 代码含敌人 AI：检测半径→追击→朝向玩家 | grep |
| A7 | 代码含 Idle/Walk 两个 AnimationClip | grep `AnimationClip`/`KeyframeTrack` |
| A8 | 代码含 health 递减 + UI 显示 + Dead 状态机 | grep |
| A9 | 命名空间合规：所有产出前缀 `${AGENT}_` | 校验唯一性 |
| A10 | `verify.sh` 存在且可运行 | 执行返回非错误码 |

### 6.2 人工验收项（主观，0–3 分，人工浏览器复核）
| # | 现象 |
|---|------|
| M1 | 打开后能加载出楼顶场景，雾+冷光营造夜晚末日氛围；**启动有由远到近的电影式开场镜头（贴近官方《求生之路》体验，无需选人）** |
| M2 | 第一人称可在楼顶自由行走、不掉图不穿墙，手感合理 |
| M3 | 门交互有可见开门动画，流程 UI 目标文字随推进更新 |
| M4 | 敌人接近改追击并播放对应动画，接触/血量表现符合预期 |

### 6.3 计分
- **客观分** = 自动项得分（满分 20）。
- **主观分** = 人工项得分（满分 12）。
- **最终排名**：先按客观分，再按主观分做 tie-break。

---

## 7. 交付物清单（每个 Agent 独立交付目录 `${AGENT}_out/` ）
1. `verify.sh`（自动验收脚本）
2. `index.html` + 模块化 `.mjs` 源码（可复现构建）
3. `${AGENT}_rooftop.glb`（Blender 导出的静态场景）
4. `${AGENT}_README.md`（构建/运行说明、命名空间声明、非目标声明）

---

## 8. 明确定义「非目标」（防止过度范围蔓延）
- 不要求完整关卡、多人、射击、投掷物、触发器战斗系统。
- 不要求 Blender 骨骼动画（用 three.js 代码动画等价承接）。
- 不要求贴图是高精度 PBR；2K 以下程序化/基础贴图即可。
- 不要求跨 Agent 协作；每个 Agent 独立完成同一题目。
