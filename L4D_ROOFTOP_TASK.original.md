目标：实现一个类似《求生之路1》第一关“楼顶开局”的小型 3D Demo，用 three.js 作为渲染与交互框架，用 Blender 进行楼顶场景和简单角色的建模与动画，具备基础的游戏流程体验。

一、整体风格与场景设定  
1. 场景主题：  
- 夜晚城市楼顶，略带雾气和冷色调灯光，营造末日、生存恐怖氛围。  
- 参考《求生之路1》No Mercy 第一关顶楼结构：主楼顶、较矮的附属屋顶、小房间、楼梯间入口、通往下层的门或平台。  

2. 场景元素：  
- 楼顶地面（有排水沟、地砖或水泥裂缝等简单贴图细节）。  
- 围栏/矮墙、空调机组、排风扇、管道、水塔、广告牌残架等。  
- 出生点附近的安全屋门（可简化为一扇可交互的门）。  
- 通往下一阶段的门或楼梯口（门可暂时锁死，仅用于指示流程终点）。  

二、建模与资源（Blender）  
1. 楼顶与静态物体建模：  
- 在 Blender 中搭建一个低/中多边形的楼顶模型，包括：地面、围栏、屋顶小房子、楼梯间入口等。  
- 为关键物体设置合理的 Pivot（例如门的 Pivot 在门铰链位置，方便在 three.js 中旋转开门）。  
- 对场景进行基本的 UV 展开，制作或应用简单的纹理（混凝土、金属、玻璃等）。  
- 将楼顶场景导出为 glTF/GLB，以便在 three.js 中直接加载。  

2. 简单角色/敌人建模与动画：  
- 建一个简化的人形模型用于敌人（僵尸），低多边形即可。  
- 绑定骨骼，制作至少两段动画：  
  - Idle/站立摇晃（简短循环）。  
  - Walk/慢走或小跑（循环）。  
- 可选：为玩家视角不展示的“玩家身体”制作极简模型（仅用于碰撞盒可在 three.js 中用 Box 表示即可，不必须建模）。  
- 敌人模型与动画同样导出为 glTF/GLB（包含 Skeleton 与 Animation Clips）。  

三、three.js 技术实现要求  
1. 基础场景搭建：  
- 初始化 three.js 场景、透视相机和 WebGLRenderer。  
- 使用一个 DirectionalLight 模拟月光，一个或多个 PointLight/SpotLight 模拟楼顶小灯光。  
- 加载楼顶 glTF 模型，放置到合适的坐标原点附近。  
- 添加轻微雾效（Fog 或 FogExp2），颜色偏蓝灰，增强氛围。  

2. 玩家控制与相机：  
- 实现第一人称视角漫游：  
  - 使用 PointerLockControls 或自定义 FPS 控制。  
  - 玩家可在楼顶范围内行走，具备碰撞检测（防止走出楼顶或穿墙，可用简单的 AABB/射线检测与阻挡）。  
- 基础操作：  
  - WASD 控制移动，鼠标移动控制视角。  
  - 空格键小跳（可选，简化重力和碰撞即可）。  

3. 基础交互逻辑：  
- 门交互：  
  - 楼顶安全屋门可被交互（例如靠近并按 “E” 键）。  
  - 交互后，播放门旋转打开动画（可在 three.js 中控制门节点绕 Pivot 旋转）。  
- 流程引导：  
  - 在界面上用简单文字 UI（HTML 或 canvas）提示当前目标，例如：  
    - “目标：离开安全屋，前往楼顶边缘。”  
    - “目标：前往楼梯口/门，离开楼顶。”  
  - 当玩家接近指定触发区（如楼梯口附近的 Box 区域），更新 UI 文本，表示第一阶段完成。  

4. 敌人（僵尸）基础 AI 与动画：  
- 从指定出生点（例如楼顶远处的阴影区域或楼梯口）实例化 2–5 个敌人。  
- 加载敌人 glTF 模型并切换动画：  
  - 初始播放 Idle 动画。  
  - 当玩家进入一定范围（Sphere 半径检测）后切换到 Walk 动画，并让敌人朝玩家方向移动。  
- 简单追踪逻辑：  
  - 每帧计算敌人到玩家位置的向量，归一化后乘以速度，更新敌人位置。  
  - 敌人保持 Y 轴旋转朝向玩家方向。  
- 碰撞与生命值（可简化）：  
  - 玩家与敌人接触时，降低玩家生命值（例如用变量 health 记录，并在屏幕上显示血量）。  
  - 当玩家血量降到 0 时，显示 “你已倒下” 文本，暂停玩家移动。  

5. 基础 UI 与状态管理：  
- 屏幕左上角显示玩家当前血量（Health: 100 → 0）。  
- 中下方用于显示提示文本（当前目标、交互提示“按 E 开门”）。  
- 实现简单的游戏状态机：  
  - Start：加载完成后显示“按任意键开始”。  
  - Playing：正常游戏状态。  
  - Dead：玩家死亡，显示“重新开始”提示。  

四、技术与结构要求  
1. 使用 ES 模块化组织代码，将以下部分拆分成模块：  
- 场景初始化（renderer、相机、灯光）。  
- 模型加载（楼顶、敌人）。  
- 玩家控制与碰撞检测。  
- 敌人 AI 与动画系统。  
- UI 状态与游戏逻辑管理。  

2. 性能与优化：  
- 控制楼顶与模型面数，保证在普通电脑浏览器中流畅运行。  
- 对纹理进行压缩与尺寸控制（如 2K 以下），合理使用重复贴图与纹理集。  

五、验收标准  
- 打开网页后，能够加载出楼顶场景，光照与雾效营造明显夜晚末日氛围。  
- 玩家可以用第一人称自由在楼顶范围移动，不会掉出地图或穿墙。  
- 至少有一扇门可以通过按键交互打开，并有明显的开门动画。  
- 敌人能在楼顶刷新，并在接近玩家时缓慢追击，播放对应动画。  
- 玩家有可见的血量数值，接触敌人后血量会下降，血量为 0 时游戏结束并有文本提示。  
- 有简单的“当前目标”文字引导楼顶流程（从安全屋→楼顶中央→楼梯口/终点触发区）。  

目标是做出一个简单但有氛围的“求生之路楼顶开局 Demo”，重点在场景氛围、基础人物移动、敌人追踪和交互门，而非完整关卡或复杂战斗系统。
